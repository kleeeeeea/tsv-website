name: Update Squad Data

on:
  workflow_dispatch:
  schedule:
    - cron: "17 * * * *"

permissions:
  contents: write

jobs:
  sync-squad-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Update FuPa squad snapshot
        run: node scripts/update-squad-data.mjs

      - name: Update FuPa season statistics
        run: node scripts/update-season-stats.mjs

      - name: Commit changes
        run: |
          if git diff --quiet -- team-data.js verein.html; then
            echo "No squad or season statistics changes"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add team-data.js verein.html
          git commit -m "chore: update FuPa data"
          git push
